#include "include/node.h"


    Node(){}; // vT{}
    Node(std::pair<const kO, vO> x){}



Node* next(Node* current){
    it = current.get();
    y.second = true;
    while (p == nullptr)
    {
        if(OP(a.first, *p.element.first) && !OP(a.first, *p.element.first){
        y.first = false;
        y.second =  p;
        p = nullptr;
    }
        else if(!OP(a.first, *p.element.first)){
            p = *p.r_next;
        }
        else if(OP(a.first, *p.element.first)){
            p = *p.l_next;
        }    
    
    }